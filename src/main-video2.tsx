import React from 'react';
import ReactDOM from 'react-dom/client';
import Video2App from './Video2App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Video2App />
  </React.StrictMode>
);
