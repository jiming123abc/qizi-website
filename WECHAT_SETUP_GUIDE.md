# 微信分享配置指南

## 概述

本指南将帮助您配置微信分享功能，让用户在微信中分享您的网站时显示美观的卡片，而不是简单的链接。

## 微信分享效果

配置完成后，微信分享将显示：
- ✅ 网站/作品标题
- ✅ 描述文字
- ✅ 缩略图片
- ✅ 点击卡片可跳转

## 前置要求

### 1. 微信公众号

您需要一个**微信公众号**（推荐使用**服务号**），订阅号的功能受限。

- 注册地址：https://mp.weixin.qq.com/
- 完成认证（需企业认证，个人无法获得高级接口）

### 2. 域名准备

- 网站必须部署到**HTTPS**域名
- 域名需要在微信公众平台配置

### 3. 服务器部署

- 网站必须先部署到公网服务器
- 后端服务必须正常运行

## 详细配置步骤

### 第一步：微信公众号配置

#### 1.1 获取AppID和AppSecret

1. 登录微信公众平台 https://mp.weixin.qq.com/
2. 进入「开发」→「基本配置」
3. 记录「开发者ID (AppID)」
4. 点击「生成」按钮，生成「开发者密码 (AppSecret)」并保存
5. ⚠️ **注意：AppSecret只显示一次，请妥善保存**

#### 1.2 配置IP白名单

1. 在「基本配置」页面，找到「IP白名单」
2. 点击「配置」
3. 添加您的服务器公网IP地址
4. 保存配置

#### 1.3 配置JS接口安全域名

1. 进入「设置」→「公众号设置」→「功能设置」
2. 找到「JS接口安全域名」
3. 点击「设置」
4. 下载验证文件（txt文件）
5. 将验证文件上传到您网站的根目录
6. 在输入框中填写您的域名（如：example.com）
7. 点击「确定」保存

#### 1.4 配置网页授权域名（可选）

如果需要获取用户信息，还需要配置：
1. 进入「设置」→「公众号设置」→「功能设置」
2. 找到「网页授权域名」
3. 同样的方式配置您的域名

### 第二步：配置环境变量

在您的服务器上，编辑 `.env` 文件，添加以下内容：

```env
# 微信公众号配置
WECHAT_APP_ID="wx你的AppID"
WECHAT_APP_SECRET="你的AppSecret"

# API地址（生产环境）
REACT_APP_API_URL="https://你的域名/api"
```

### 第三步：服务器部署

确保您的后端服务正常运行，并且可以访问 `/api/wechat/signature` 接口。

### 第四步：OSS图片配置

确保分享的图片符合要求：
- 图片格式：JPG、PNG
- 图片大小：建议 300x300 像素
- 图片大小不超过 300KB
- 图片必须是公网可访问的HTTPS地址

## 功能说明

### 已实现的功能

1. **Meta标签设置**
   - Open Graph标签
   - 微信专用标签
   - Twitter Card标签

2. **微信JS-SDK配置**
   - 自动获取签名
   - 自动缓存ticket（7000秒）
   - 兼容新旧API

3. **分享功能**
   - 分享给朋友
   - 分享到朋友圈
   - 自定义标题、描述、图片

### 代码说明

#### 后端接口 (`server/server.js`)

- `GET /api/wechat/signature?url=xxx` - 获取微信签名
- 自动管理access_token和jsapi_ticket缓存

#### 前端工具 (`src/lib/shareUtils.ts`)

- `isWeChat()` - 检测是否在微信环境
- `injectWeChatSDK()` - 注入微信JS-SDK
- `setupWeChatShare(config)` - 配置分享内容

## 测试微信分享

### 本地测试（开发环境）

1. 安装微信开发者工具：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
2. 在微信开发者工具中打开您的网站
3. 打开「调试」→「微信开发者工具」
4. 检查是否有错误信息

### 真机测试

1. 使用微信扫码访问您的网站
2. 点击右上角「...」
3. 选择「分享给朋友」
4. 查看是否显示卡片

## 常见问题

### Q1: 分享还是显示链接，不显示卡片？

**可能原因：**
1. 域名未配置JS接口安全域名
2. 签名验证失败
3. 图片地址无法访问
4. AppID或AppSecret配置错误

**解决方法：**
1. 检查JS接口安全域名配置
2. 查看浏览器控制台错误信息
3. 确认环境变量配置正确
4. 检查图片URL是否可访问

### Q2: 分享图片不显示？

**可能原因：**
1. 图片不是HTTPS地址
2. 图片尺寸过大
3. 图片格式不支持
4. 图片跨域限制

**解决方法：**
1. 确保图片使用HTTPS
2. 图片建议尺寸300x300px
3. 使用JPG或PNG格式
4. 在OSS配置CORS

### Q3: 签名失败？

**可能原因：**
1. IP不在白名单
2. AppID或AppSecret错误
3. URL签名不匹配
4. ticket过期

**解决方法：**
1. 检查IP白名单配置
2. 确认环境变量配置
3. 检查URL是否正确（不含#后面的内容）
4. 查看后端日志

### Q4: 如何调试？

1. 在 `shareUtils.ts` 中设置 `debug: true`
2. 打开微信开发者工具
3. 查看Console输出
4. 查看Network请求

## 安全建议

1. **不要将AppSecret提交到代码仓库**
2. **使用环境变量管理敏感信息**
3. **定期更换AppSecret**
4. **限制服务器IP访问**
5. **监控API调用次数**

## 微信官方文档

- JS-SDK文档：https://developers.weixin.qq.com/doc/offiaccount/OA_Web_Apps/JS-SDK.html
- 微信分享接口：https://developers.weixin.qq.com/doc/offiaccount/OA_Web_Apps/JS-SDK.html#48

## 快速检查清单

部署后，请检查：

- [ ] 微信公众号已认证
- [ ] AppID和AppSecret已配置
- [ ] IP白名单已添加
- [ ] JS接口安全域名已配置
- [ ] 后端服务正常运行
- [ ] 环境变量已设置
- [ ] 网站使用HTTPS
- [ ] 分享图片可访问
- [ ] 真机测试通过

需要帮助？请查看微信官方文档或联系技术支持。
